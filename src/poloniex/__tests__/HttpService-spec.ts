// import * as httpService from '../http-queue.service';
// import { RequestGetQueue } from '../http-queue.service';

describe('should show data from poloniex exchange', () => {
    it.todo('should be executes requests by 500 mili', () => {
        // let isDoned = false;
        // function displayData(data){
        //     console.log(data.result);
        //     if(httpService.__queue.length==0 && !isDoned){
        //         isDoned = true;
        //         expect(true, 'ok');
        //         done();
        //     }
        // };

        // function displayError(data){
        //     console.log(data.error);
        //     if(httpService.__queue.length==0 && !isDoned){
        //         isDoned = true;
        //         expect(true, 'ok');
        //         done();
        //     }
        // };
        // const url = 'https://poloniex.com/public?command=returnChartData&currencyPair=BTC_BCN&start=1500624736.83&end=9999999999&period=14400'
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);
        // httpService.enqueue(new RequestGetQueue(url)).then(displayData).catch(displayError);

    });
    
});
