// import { PoloniexApi } from '../../poloniex/poloniex.service';
// import { SmartTradeStrategy } from '../SmartTradeStrategy';
// import { ITicker } from '../../interfaces/models';


// const api = new PoloniexApi(_key, _secret);

describe('should show best tickers to smart trading', () => {
    // const strategy = new SmartTradeStrategy(api);
    // let ranking: ITicker[];
    it.todo('should be ranking best volume coin pairs', async () => {
        // ranking = await strategy.getRankingVolume(['BTC']);
        // console.log(ranking);
        // expect(ranking).toBeTruthy()
    });

    it.todo('should show average low to buy', async () => {
        
        // const chartData = await api.returnChartData({currencyPair: ranking[0].name, start: moment().subtract(30, 'minute').toDate(), period: "5m"});
        // const avgClose = from(chartData).average(c => c.close);
        // const avgLow = from(chartData).average(c => c.low);

        // console.log(`${avgClose.toFixed(8)}, ${avgLow.toFixed(8)}`);
    });

    it.todo('should show if while value still upping', async () => {
        
        // const chartData = await api.returnChartData({currencyPair: ranking[0].name, start: moment().subtract(30, 'minute').toDate(), period: "5m"});
        // const avgClose = from(chartData).average(c => c.close);
        // const avgLow = from(chartData).average(c => c.low);

        // console.log(`${avgClose.toFixed(8)}, ${avgLow.toFixed(8)}`);
    });

    
});