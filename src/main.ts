// // import { CacheUpdater } from './cache/updater.service';
// // import tickerCache from './cache/ticker'


// setInterval(() => { 
//     console.clear()
//     // const tickers = tickerCache.all().filter(t => t.currencyPair.startsWith('BTC_'))
//     // console.table(tickers.sort((a, b) => {
//     //     return a.currencyPair.localeCompare(b.currencyPair)
//     // }))
// }, 1000)

// const updater = new CacheUpdater({
//     subscribes: [
//         // 'ticker', 
//         'volume'
//     ]
// })

// updater.start()
